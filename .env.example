# DGUT Ulearning 认证配置
# 从浏览器开发者工具中获取 token 或 AUTHORIZATION Cookie 的值

# 认证 Token（必填）
# 获取方式：登录 ua.dgut.edu.cn 后，在浏览器开发者工具 -> Application -> Cookies 中找到 token 的值
AUTHORIZATION=114514ABCDEFG

# 学习记录同步时使用的用户名（可选；命令行未显式传 --user-name 时读取这里）
# ULEARNING_USER_NAME=张三